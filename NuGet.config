<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <!-- This project has no package references. Clearing inherited sources
       keeps restore offline and avoids the machine-wide private feed, which
       requires credentials this build does not need. -->
  <packageSources>
    <clear />
  </packageSources>
</configuration>
